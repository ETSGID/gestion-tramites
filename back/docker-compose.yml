version: '3'

services: 

  dbsession:
    image: postgres:9.6
    volumes:
      - dbdata2:/var/lib/postgres
    restart: always
    
  gestionTitulos:
    build: 
      context: .
    restart: always
    volumes:
      - gestionTitulos:/storage/gestionTitulos/
    ports: 
      - "3000:3000"
    depends_on:
      - dbsession

volumes:
  dbdata2:
  gestionTitulos:
